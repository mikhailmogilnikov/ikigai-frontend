schemas:
  Transaction:
    description: Транзакция в списке транзакций ученика
    type: object
    properties:
      id:
        type: number
      invoice_id:
        type: string
      date:
        type: string
        format: date-time
      amount:
        type: number
      title:
        type: string
    required:
      - id
      - invoice_id
      - date
      - amount
      - title
