schemas:
  AdminModule:
    description: Модуль для админа
    allOf:
      - $ref: "../../shared/schemas/base-course-schemas.yaml#/schemas/BaseModule"
      - type: object
        properties:
          is_published:
            type: boolean
          lessons_count:
            type: number
        required:
          - is_published
          - lessons_count